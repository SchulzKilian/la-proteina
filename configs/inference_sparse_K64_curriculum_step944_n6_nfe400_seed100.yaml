defaults:
  - inference_base
  - generation: uncond_codes_ca_only
  - _self_

run_name_: laproteina_sparse_K64_curriculum_step944_n6_nfe400_seed100
config_name: inference_sparse_K64_curriculum_step944_n6_nfe400_seed100

# E047b — same K=64 curriculum-trained ckpt as E047, re-run at seed=100 so the
# paired-by-noise comparison can use the canonical N=30 (seed=100) and
# sparse_K40 N=30 (seed=100) references. The seed=5 E047 run is paired with
# E044/E045/E046 only.
ckpt_path: /home/ks2218/la-proteina
ckpt_name: best_val_00000009_000000000944.ckpt
autoencoder_ckpt_path:

seed: 100

generation:
  args:
    nsteps: 400
    curriculum_neighbors: True
  dataset:
    nsamples: 6
    max_nsamples_per_batch: 6
    nlens_cfg:
      nres_lens: [50, 100, 200]
