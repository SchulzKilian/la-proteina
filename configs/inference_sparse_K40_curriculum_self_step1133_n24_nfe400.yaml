defaults:
  - inference_base
  - generation: uncond_codes_ca_only
  - _self_

run_name_: laproteina_sparse_K40_curriculum_self_step1133_n24_nfe400
config_name: inference_sparse_K40_curriculum_self_step1133_n24_nfe400

# N=24 × L∈{50,100,200} × nsteps=400 extension of E077 (N=6 curric-ON).
# Same ckpt (`sparse_K40_curriculum_self_step1133.ckpt`), same seed=5
# (inherited from inference_base.yaml), trained-in curriculum auto-fires
# from saved cfg_exp. Output dir distinct from E077 so the 6-sample probe
# is preserved.
ckpt_path: /home/ks2218/la-proteina
ckpt_name: sparse_K40_curriculum_self_step1133.ckpt
autoencoder_ckpt_path:

generation:
  dataset:
    nsamples: 24
    max_nsamples_per_batch: 6
    nlens_cfg:
      nres_lens: [50, 100, 200]
