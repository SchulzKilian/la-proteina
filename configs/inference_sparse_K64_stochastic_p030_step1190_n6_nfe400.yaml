defaults:
  - inference_base
  - generation: uncond_codes_ca_only
  - _self_

run_name_: laproteina_sparse_K64_stochastic_p030_step1190_n6_nfe400
config_name: inference_sparse_K64_stochastic_p030_step1190_n6_nfe400

# Canonical N=6 × L∈{50,100,200} × nsteps=400 designability probe on the
# `ca_only_sparse_K64_stochastic_p030` training run, step 1190
# (best_val_00000005_000000001190.ckpt, rsynced 2026-05-25 07:39).
#
# This is the FIRST probe of the stochastic_k variant (stochastic_k=0.30 —
# ~30% of training forward passes swap K=full instead of K=64).
# stochastic_k is a TRAINING-TIME knob only: at inference the model uses
# the canonical K=64 SALAD split (n_seq=8 per side → 16 sequential,
# n_spatial=16, n_random=32; K=64 total), identical to every other K=64 ckpt.
#
# NN config `nn/ca_only_sparse_K64_curriculum_160M.yaml` is static 16/16/32.
# `curriculum_neighbors: False` is a no-op for this ckpt (it never trained
# with curriculum), kept for parity with the template.
#
# nsteps inherited from `inference_base.yaml` (=400) per CLAUDE.md hard rule.
ckpt_path: /home/ks2218/la-proteina
ckpt_name: sparse_K64_stochastic_p030_step1190.ckpt
autoencoder_ckpt_path:

generation:
  args:
    curriculum_neighbors: False
  dataset:
    nsamples: 6
    max_nsamples_per_batch: 6
    nlens_cfg:
      nres_lens: [50, 100, 200]
