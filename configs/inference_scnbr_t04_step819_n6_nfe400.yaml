defaults:
  - inference_base
  - generation: uncond_codes_ca_only
  - _self_

run_name_: laproteina_scnbr_t04_step819_n6_nfe400
config_name: inference_scnbr_t04_step819_n6_nfe400

# E038 redo at nsteps=400 (Fix C2 active per the local ckpt; the variant arm
# `ca_only_sparse_K40_scnbr_t04` at ep=8 step=819). Original at nsteps=200.
ckpt_path: /home/ks2218/la-proteina
ckpt_name: best_val_00000008_000000000819.ckpt
autoencoder_ckpt_path:

generation:
  args:
    nsteps: 400
  dataset:
    nsamples: 6
    max_nsamples_per_batch: 6
    nlens_cfg:
      nres_lens: [50, 100, 200]
