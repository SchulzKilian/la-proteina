defaults:
  - inference_base
  - generation: uncond_codes_ca_only
  - _self_

run_name_: laproteina_hybrid_conv_to_scnbr_t06_n6_nfe400
config_name: inference_hybrid_conv_to_scnbr_t06_n6_nfe400

# E040 redo at nsteps=400 (and bumped from N=3 to N=6 per user directive).
# Hybrid: conv (1D-downsampling) for t < t_switch, scnbr_K40 + Fix C2 for
# t >= t_switch. Original probe was at nsteps=200.
ckpt_path_a: /home/ks2218/la-proteina   # conv (early, t < t_switch)
ckpt_name_a: best_val_00000023_000000002331.ckpt
ckpt_path_b: /home/ks2218/la-proteina   # scnbr_K40_t04 (late, t >= t_switch)
ckpt_name_b: best_val_00000011_000000001133.ckpt
t_switch: 0.6

ckpt_path: /home/ks2218/la-proteina
ckpt_name: best_val_00000023_000000002331.ckpt
autoencoder_ckpt_path:

generation:
  args:
    nsteps: 400
    sc_neighbors_bootstrap: False
  dataset:
    nsamples: 6
    max_nsamples_per_batch: 6
    nlens_cfg:
      nres_lens: [50, 100, 200]
