defaults:
  - inference_base
  - generation: uncond_codes_ca_only
  - _self_

run_name_: laproteina_downsampled_quick
config_name: inference_downsampled_quick

# Checkpoint rsynced from HPC:
#   /rds/user/ks2218/hpc-work/store/ca_only_downsampled/1777987722/checkpoints/
ckpt_path: /home/ks2218/la-proteina
ckpt_name: best_val_00000023_000000002331.ckpt
autoencoder_ckpt_path:

# Probe: 3 lengths × 6 samples × 200 ODE steps (matches E021 protocol).
generation:
  args:
    nsteps: 400
  dataset:
    nsamples: 6
    max_nsamples_per_batch: 6
    nlens_cfg:
      nres_lens: [50, 100, 200]
