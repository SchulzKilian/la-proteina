defaults:
  - inference_base
  - generation: uncond_codes_ca_only
  - _self_

run_name_: laproteina_scnbr_t04_step1133_n6_nfe400
config_name: inference_scnbr_t04_step1133_n6_nfe400

# E039 redo at nsteps=400. Variant arm `ca_only_sparse_K40_scnbr_t04` at
# ep=11 step=1133 (best_val). Original probe was at nsteps=200.
# Filename collision: this filename previously held E021's sparse+pairupdate
# ckpt — verify run_name_ from hparams before re-running E021.
ckpt_path: /home/ks2218/la-proteina
ckpt_name: best_val_00000011_000000001133.ckpt
autoencoder_ckpt_path:

generation:
  args:
    nsteps: 400
  dataset:
    nsamples: 6
    max_nsamples_per_batch: 6
    nlens_cfg:
      nres_lens: [50, 100, 200]
