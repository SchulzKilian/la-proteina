defaults:
  - inference_base
  - generation: uncond_codes_ca_only
  - _self_

run_name_: laproteina_sparse_K40_step1259_baseline_n6_nfe400
config_name: inference_sparse_K40_step1259_baseline_n6_nfe400

# E0NN baseline arm — plain sparse K=40 ckpt @ step 1259 (best_val for that variant).
# Companion: inference_sparse_K40_step1259_curriculum_n6_nfe400.yaml. Identical except
# the curriculum_neighbors flag.
ckpt_path: /home/ks2218/la-proteina
ckpt_name: sparse_K40_step1259.ckpt
autoencoder_ckpt_path:

generation:
  args:
    nsteps: 400
  dataset:
    nsamples: 6
    max_nsamples_per_batch: 6
    nlens_cfg:
      nres_lens: [50, 100, 200]
