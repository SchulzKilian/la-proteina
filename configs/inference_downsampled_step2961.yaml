defaults:
  - inference_base
  - generation: uncond_codes_ca_only
  - _self_

run_name_: laproteina_downsampled_step2961
config_name: inference_downsampled_step2961

# Checkpoint rsynced from HPC (run ca_only_downsampled, opt step 2961).
ckpt_path: /home/ks2218/la-proteina
ckpt_name: best_val_00000029_000000002961.ckpt
autoencoder_ckpt_path:

# Probe: 3 lengths × 6 samples × 400 ODE steps (canonical bar; see memory
# feedback_use_nsteps_400_for_designability.md — nsteps=200 handicaps variants).
generation:
  args:
    nsteps: 400
  dataset:
    nsamples: 6
    max_nsamples_per_batch: 6
    nlens_cfg:
      nres_lens: [50, 100, 200]
