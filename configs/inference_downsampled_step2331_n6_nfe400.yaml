defaults:
  - inference_base
  - generation: uncond_codes_ca_only
  - _self_

run_name_: laproteina_downsampled_step2331_n6_nfe400
config_name: inference_downsampled_step2331_n6_nfe400

# E034 redo at nsteps=400 (the original was at nsteps=200, below the integrator-
# convergence bar). Same ckpt, same seed, same N=6, same lengths — the only
# delta vs E034 is nsteps. See CLAUDE.md "Sampling — nsteps=400 is a HARD RULE".
ckpt_path: /home/ks2218/la-proteina
ckpt_name: best_val_00000023_000000002331.ckpt
autoencoder_ckpt_path:

generation:
  args:
    nsteps: 400
  dataset:
    nsamples: 6
    max_nsamples_per_batch: 6
    nlens_cfg:
      nres_lens: [50, 100, 200]
