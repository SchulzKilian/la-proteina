defaults:
  - inference_base
  - generation: uncond_codes_ca_only
  - _self_

run_name_: laproteina_sparse_K40_curriculum_self_step1133_n6_nfe400
config_name: inference_sparse_K40_curriculum_self_step1133_n6_nfe400

# Canonical N=6 × L∈{50,100,200} × nsteps=400 designability probe of the
# K=40 + curriculum + self-inclusion variant — K=40 cousin of §11 (K=64).
#
# Ckpt: `best_val_00000011_000000001133.ckpt` (epoch 11, opt step 1133).
# run_name_ in the ckpt: `ca_only_sparse_K40_curriculum_self`.
# Neighbor block: n_seq=8, n_spatial=8, n_random=16 → K=40 per query.
# NN architecture replays from the ckpt's saved cfg_exp — no NN override
# needed. nsteps inherited from `inference_base.yaml` (=400; CLAUDE.md hard rule).
ckpt_path: /home/ks2218/la-proteina
ckpt_name: sparse_K40_curriculum_self_step1133.ckpt
autoencoder_ckpt_path:

generation:
  dataset:
    nsamples: 6
    max_nsamples_per_batch: 6
    nlens_cfg:
      nres_lens: [50, 100, 200]
