defaults:
  - inference_base
  - generation: uncond_codes_ca_only
  - _self_

run_name_: laproteina_sparse_pairupdate_quick
config_name: inference_sparse_pairupdate_quick

# Checkpoint rsynced from HPC:
#   /rds/user/ks2218/hpc-work/store/ca_only_sparse_K40_pairupdate/1777463843/checkpoints/
ckpt_path: /home/ks2218/la-proteina
ckpt_name: best_val_00000011_000000001133.ckpt
autoencoder_ckpt_path:

# Probe: 3 lengths × 6 samples × 400 ODE steps. nsteps=400 is the production
# minimum (CLAUDE.md "Sampling — nsteps=400 is a HARD RULE"); flipped 2026-05-07.
generation:
  args:
    nsteps: 400
  dataset:
    nsamples: 6
    max_nsamples_per_batch: 6
    nlens_cfg:
      nres_lens: [50, 100, 200]
