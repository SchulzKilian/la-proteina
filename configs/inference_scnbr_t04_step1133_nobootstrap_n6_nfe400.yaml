defaults:
  - inference_base
  - generation: uncond_codes_ca_only
  - _self_

run_name_: laproteina_scnbr_t04_step1133_nobootstrap_n6_nfe400
config_name: inference_scnbr_t04_step1133_nobootstrap_n6_nfe400

# E084: bootstrap-disabled rerun of E083 on the same trained-with-Fix-C2 ckpt
# (ca_only_sparse_K40_scnbr_t04, step 1133). Single delta vs E083:
# sc_neighbors_bootstrap: false. Tests whether the step-0 bootstrap-x_sc
# forward is what's poisoning the integration trajectory at nsteps=400.
# If E084 ≫ E083's 1/18, bootstrap is the failure mode and the default in
# inference_base.yaml:23 is wrong for Fix-C2-trained ckpts.
ckpt_path: /home/ks2218/la-proteina
ckpt_name: best_val_00000011_000000001133.ckpt
autoencoder_ckpt_path:

generation:
  args:
    nsteps: 400
    sc_neighbors_bootstrap: false   # override inference_base.yaml:23 default of True
  dataset:
    nsamples: 6
    max_nsamples_per_batch: 6
    nlens_cfg:
      nres_lens: [50, 100, 200]
