defaults:
  - inference_base
  - generation: uncond_codes_ca_only
  - _self_

run_name_: laproteina_sparse_K64_curriculum_self_step1385_n6_nfe400
config_name: inference_sparse_K64_curriculum_self_step1385_n6_nfe400

# Convergence probe for the K=64 SALAD-canonical sparse + low-t→SALAD curriculum
# + self-inclusion variant (group `ca_only_sparse_K64_curriculum_self`,
# wandb chain euiryxd4 → 7sdu834p → rmuumq8v). Step 1385 set the all-time
# best val (4.1908) IN the last continuation, so this probe answers
# "is the val improvement real, or just MSE noise?"
#
# nsteps inherited from inference_base.yaml (=400). DO NOT override downward.
# Curriculum + self-inclusion are baked into the saved exp_config inside the
# ckpt; LocalLatentsTransformer reads `curriculum_neighbors=True` and the K=64
# SALAD-canonical (8/16/32) split from kwargs. Don't add curriculum overrides
# here or the schedule diverges from training.
ckpt_path: /home/ks2218/la-proteina
ckpt_name: sparse_K64_curriculum_self_step1385.ckpt
autoencoder_ckpt_path:

generation:
  dataset:
    nsamples: 6
    max_nsamples_per_batch: 6
    nlens_cfg:
      nres_lens: [50, 100, 200]
