defaults:
  - inference_base
  - generation: uncond_codes_ca_only
  - _self_

run_name_: laproteina_canonical_step2646_n6_nfe400
config_name: inference_canonical_step2646_n6_nfe400

# Canonical-alone at step 2646, N=6 × L∈{50,100,200} × seed=5 × nsteps=400.
# Reference probe for the E041/E088/E089 hybrid value-add disambiguation.
# E019 has step 2646 at N=30 but different seed; this entry locks the seed=5
# N=6 number for clean delta-vs-hybrid analysis at matched protocol.
# Previous version of this YAML had nres_lens=[50, 100] only — L=200 added
# 2026-05-26 to complete the comparison; old CSV preserved as
# results_*.csv.preL200_backup.
ckpt_path: /home/ks2218/la-proteina
ckpt_name: best_val_00000026_000000002646.ckpt
autoencoder_ckpt_path:

generation:
  args:
    nsteps: 400
  dataset:
    nsamples: 6
    max_nsamples_per_batch: 6
    nlens_cfg:
      nres_lens: [50, 100, 200]
