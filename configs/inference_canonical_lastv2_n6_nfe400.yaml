defaults:
  - inference_base
  - generation: uncond_codes_ca_only
  - _self_

run_name_: laproteina_canonical_lastv2_n6_nfe400
config_name: inference_canonical_lastv2_n6_nfe400

# Canonical CA-only baseline (run 1776805213) — last-v2.ckpt rsynced 2026-05-10.
# This is a later step on the same baseline run whose best_val ckpt is
# best_val_00000026_000000002646.ckpt (CLAUDE.md "best ckpt on disk").
# N=6 × L∈{50,100,200} × nsteps=400 designability probe — the canonical bar.
ckpt_path: /home/ks2218/la-proteina
ckpt_name: last-v2.ckpt
autoencoder_ckpt_path:

generation:
  dataset:
    nsamples: 6
    max_nsamples_per_batch: 6
    nlens_cfg:
      nres_lens: [50, 100, 200]
